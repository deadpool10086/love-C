#include <stdio.h>
#include <stdlib.h>

/* run this program using the console pauser or add your own getch, system("pause") or input loop */

int main(int argc, char *argv[]) {
	FILE*inp,*outp;
	if(imp=fopen("d:\\input\\test.txt","r")==0)
	{
		printf("errior");
		exit(0);
	}
	fseek()
	return 0;
}
